{
  "name": "sakhisilai-platform",
  "version": "1.0.0",
  "description": "SakhiSilai Hyperlocal Women Tailoring Platform - Frontend (Vercel) & Backend (Render)",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && npm run build",
    "start:backend": "cd backend && npm start"
  }
}
